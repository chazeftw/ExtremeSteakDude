<UserControl x:Class="ExtremeSteakDude.View.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExtremeSteakDude.View"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid>
        <Button x:Name="NewGame" Content="NewGame" Margin="354,160,0,0" Command="{Binding NewGameCommand}" />
        <Button x:Name="Continue" Content="Continue" Margin="354,290,0,0" Command="{Binding ContinueCommand}"/>
        <Button x:Name="HighScore" Content="HighScore" Margin="354,420,0,0" Command="{Binding HighScoreCommand}"/>
        <Button x:Name="Exit" Content="Exit" Margin="354,550,0,0" Command="{Binding ExitCommand}"/>
        <Button x:Name="Test" Content="Test" Margin="69,372,0,0" Command="{Binding NewHighScoreCommand}"  Width="195" Height="89"/>

    </Grid>
</UserControl>
